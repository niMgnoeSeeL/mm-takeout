# Copy to .env and fill in. .env is gitignored.
MM_URL=https://chat.example.com
MM_TOKEN=

# Optional
# MM_OUT=./export        # where the archive is written
# MM_TYPES=ODPG          # O public, P private, D DMs, G group DMs
# WORKERS=5              # parallel attachment downloads
